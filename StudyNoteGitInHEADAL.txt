git config --global user.name
git config --global user.email
git config --list 
git config --global 

git init

git remote add origin main
git remote -v

git add <. 파일명>
git commit -m <커밋 메세지>
git push 

git checkout <바꿀 브랜치명>
git switch <바꿀 브랜치명>
git merge <병합할 브랜치명>

git branch       //브랜치 리스트 확인       
git branch <새로 만들 브랜치명> //새 브랜치 생성

git pull
git fetch

git clone <깃허브 레포지토리 주소> //해당 레포 파일 전부 받아옴

git status

git log //버전 내역 


